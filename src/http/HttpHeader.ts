export enum HttpHeader {
    ContentType = "Content-Type",
    Accept = "Accept",
    Allow = "Allow",
    Location = "Location",
    Link = "Link",
    ETag = "ETag",
    IfMatch = "If-Match",
    IfNoneMatch = "If-None-Match"
}
